/**
 * Server-safe virtual file system backed by an in-memory Map.
 * Thin adapter over {@link VirtualFileSystem} — file semantics live in one place.
 */
export { ServerVirtualFileSystem } from './filesystem';
